import { LoginSchema } from '@/components/client/login-schema';
import React from 'react';

function page(props) {
    return (
        <div>
            <LoginSchema />
        </div>
    );
}

export default page;